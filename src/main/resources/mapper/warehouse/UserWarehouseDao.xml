<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.database.modules.warehouse.dao.UserWarehouseDao">

    <resultMap type="com.example.database.modules.warehouse.entity.UserWarehouseEntity" id="userWarehouseMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="warehouseId" column="warehouse_id"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="creator" column="creator"/>
        <result property="createDate" column="create_date"/>
    </resultMap>
    <select id="getWarehouseIdByUserId" resultType="String">
		select warehouse_id from user_warehouse where user_id in
        <foreach item="userId" collection="array" open="(" separator="," close=")">
            #{userId}
        </foreach>
	</select>

    <select id="getUserIdByWarehouseId" resultType="String">
        select distinct user_id from user_warehouse where warehouse_id in
        <foreach item="warehouseId" collection="array" open="(" separator="," close=")">
            #{warehouseId}
        </foreach>
    </select>

    <select id="isData" resultType="int">
        select 1 from user_warehouse
        <where>
            true
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="warehouseId != null">
                and warehouse_id = #{warehouseId}
            </if>
        </where>
        limit 1
    </select>

    <select id="getByUserWarehouse" resultType="com.example.database.modules.warehouse.entity.UserWarehouseEntity">
        select * from user_warehouse
        <where>
            user_id = #{userId} and warehouse_id = #{warehouseId}
        </where>
        limit 1
    </select>

    <delete id="deleteByUserWarehouse">
        delete from user_warehouse
        <where>
            user_id = #{userId}
            and warehouse_id in
            <foreach item="warehouseId" collection="warehouseId" open="(" separator="," close=")">
                #{warehouseId}
            </foreach>
        </where>
    </delete>
</mapper>